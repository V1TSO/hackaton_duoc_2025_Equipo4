# HealthAI Backend

### Stack:
- FastAPI (API principal)
- Supabase (auth y BD)
- OpenAI (coach IA)
- Colab (modelo ML)

### Ejecutar localmente:
```bash
pip install -r requirements.txt
uvicorn main:app --reload
